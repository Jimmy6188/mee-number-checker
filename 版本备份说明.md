# MEE 多国语数字校对工具 - 版本备份说明

## 备份时间
2026-08-31 (锚定方案改造前)

## 备份内容
| 文件 | 说明 |
| --- | --- |
| `mee_checker.py` | **当前开发版 (v1.1 锚定方案改造起点,未提交)** |
| `mee_checker_v1.0_全量扫描版_20260831.py` | **v1.0 全量扫描版备份**, 与 git 首提交一致 |
| `运行校对_v1.0_全量扫描版_20260831.bat` | v1.0 启动脚本备份 |


## v1.7 三档人工审核视图 + 对照册 + 判定层对称修复 (2026-09-18~20)

### 判定层(会改变判定结果)
- **页级计数对账补对称**: 之前只给"英文有/译文找不到"侧做了同值计数对账, "译文多出"侧只做存在性判断
  → 译文重复排版一整段(如匈牙利语 Wi-Fi 安全间距条款出现两次)被降成中风险。现补上: 译文侧同值次数 > 英文侧 → 真多余(高风险)
  验证: F0295 中风险 96→78、高风险 4→22, 4 条已知植入错与 Es 15 缺失全部保留; A0404/J0105 零变化
- **跨语言同现合并**: 同一(页,值,偏多差)在 ≥3 个语言同时偏多 → 视为该页标注/排版系统性现象, 折叠成一张同类卡(仍高风险), 单语言独有逐条必看
  验证: F0295 高风险 22→7(呈现层必看从 44 条压到 31 条)
- **NFKC 归一**: `²`→`2`、`³`→`3`、全角数字、`，`→`,`; 单位指纹同时去 `^`(cm^2/cm²/cm2 同写法)。F0295 净 +2 一致 / -3 中风险
- **上标排版剔除**: 单字符数字 + 字号 < 参照×0.8 + 基线上移 >1pt → 与 `²` 字符同等剔除(两侧必须同判据, 否则产生假"译文多出")
  实测判据区分度: `kgf/cm²` Δ基线 -2.33(上标, 剔) vs `CO₂` Δ基线 +0.33(化学式, 保留转人工)
- **意译归因**: 英文同页非红正文含数字词(one/two/…) 且译文该值计数偏多 → 备注写「疑英文数字词意译(two→2)」, 不改档位
- **宿主词归因**: Item 新增 host(红字段被哪个字母数字词包住, 如 CO2/MXZ233VG), 真缺失备注写「数字嵌于CO2内」; 只用于归因, 不作降档依据
- **页数不一致不再崩溃**: `align_entry_columns_spans` 对越界页 IndexError 是同事在 GUI 里看到的报错; 改为只对两侧都存在的页做编号列对齐 + snap 越界兜底
- **逐文件诊断**: 打不开 / 页数与指示稿不一致(写明本文件几页、指示稿几页、多或少几页) / 未找到任何红字 / 目录无可校译文 → 即时落盘 `文件诊断.txt`(中途失败不丢) + 日志 + HTML 顶部诊断区 + GUI 弹窗

### 呈现层(不改判定, 四组 A/B 全部 0 差异)
- **三档预设视图**取代风险筛选按钮: 只抓铁证 / 标准档(推荐) / 全面, 按钮上显示**本轮实测算出的条数与分钟**, 默认标准档, 本机记住上次选择; 无任何输入框(同事不填参数)
- **首屏语言红绿表** + 语言名带文件缩写「德语 (02De)」便于回目录找文件
- **同类卡**: 按(成因,页)归组, 卡数 >10 自动按成因单键合并; 含高风险的卡默认可见 2 条样例且**必须整类确认**; 二次确认防手滑; 撤销一次生效
- **命中即炸开**: 卡内任一条(含哨兵)被标"确定是错" → 该卡 done 自动作废 + 红标题 + 禁用整类确认 + 自动展开 + 面板警告与跳转; 撤销错误标记则自动解除并留痕 `X@`
- **哨兵抽样重写**: 旧版按语言字母序取前 k 条, 实测 17 语言只覆盖 4 个(同一语言被重复抽) → 改为按卡规模分级(≤5→1/6~20→2/>20→3, 全报告封顶 12) + 卡内跨语言轮转 + 跳卡优先未抽语言 + 同语言取 |y偏移| 与低置信最可疑的一条 + 起点由报告指纹决定(同数据可复现, 跨项目轮换)
  覆盖率: F0295 4→8 语言, J0105 红字 3→4, 高亮 4→7
- **双语对照册.pdf**: 必看队列每条一页 = 英文定位页 | 译文定位页 并排, 复用"每问题独占一页+黄底红边"既有渲染, 不另画编号/连线(同页多框会叠加遮挡); 红边由黑色误描改为显式 RED width=0.9(PyMuPDF finish 不写 color 时默认黑描边)
- **框外值差异独立清单**: 顶部一行「⚠ 框外值差异 N 处」可展开(语言/检查点/页/英文值→译文值), 不计入必办但不再丢
- **人工结论 CSV 21 列**: 档/卡片id/语言/检查点/页/英文值/译文值/工具判定/成因/置信度/y偏移/完整备注/结论/标记时间/会话开始/工具版本/报告指纹/卡内条目数/卡内哨兵数/本类是否命中真错/本类曾标错后撤销
- 右侧悬浮审核面板: 进度条 + 已看/待看/标为错 + 已标错清单(可跳转) + 跳到下一条未看 + 卡片进度 + 标签页标题计数, 可折叠记忆

### 回归基线变化 (对照 v1.6.1 提交 63e95b2)
| 数据集 | 一致 | 低 | 中 | 高 | 必看/卡 |
|---|---|---|---|---|---|
| F0295 17语言 | 8004→7972 | 29 | 96→70 | 4→**7** | 30 条 / 3 卡 |
| A0404 24语言 | 181→252 | 0 | 6→2 | 10→**12** | 13 条 / 2 卡 |
| J0105 红字 | 1215→1211 | 11 | 19→6 | 5→**8** | 12 条 / 3 卡 |
| J0105 高亮(暂停) | 1901 | 0 | 119 | 4 | 零变化 |

A0404 的 +2 是制冷剂表里的上标 `2` 剔除后, 原本被聚成一坨的植入错(`25→55`、`2.7→2.8`)首次拆出来独立报出, 属召回提升。

### 已知残留(如实)
- **意大利语第2页 3 条 `mm²` 假阳性**: 该 PDF 把上标 `2` 单独切成一个 block, 同 line 内无其它 span → 找不到参照 → 上标判据未执行。
  曾尝试把参照放宽到"同行带左侧 ≤40pt", 结果 F0295 高风险 7→14、J0105 8→19 且方向反转(两侧切行结构不同导致同一判据命中对象不同, 破坏对称性), **已回退**。
  待试方案: 改用全文档统计量(正文众数字号 + 正文众数基线), 不依赖任何参照 span。
- 哨兵对超大卡仍是低概率探测(51 条抽 3, 藏 1 错命中率 6%), 真正兜底靠"含高风险卡必须看样例 + 命中即炸开 + CSV 回流收紧成因"。
- 估时参数(必看 40s/卡 45s)未经同事实测校准, 只影响按钮上的分钟数。

## v1.6.1 真缺失判定收紧 + 页级计数对账证据 (编码 2026-09-15 / 回归通过 2026-09-18)

### 变更
- 分支B 粘连收养仅限 `status=='译文未匹配'`; 多候选改取 y 最近者, 最近与次近差 <5pt 仍判歧义不合并
- 聚合方向要求非空"注释标号前缀"(`dh>=1`): 堵住英文型号碎片 `2 33`(MXZ-2G33VG) 被当成检查点 `2` 的配对而假"一致"
- 串位候选必须未被其它检查点配对(`id(it) in paired_xx` 则跳过), 避免 CO₂ 下标 2 被附近 (2) 列表号误救
- 新增**页级同值计数对账** `_count_val_on_page`: 同值项已全部被配对时, 译文侧该值出现次数 < 英文侧 → 真缺失(高风险); 否则为排列/配对歧义 → 中风险
- 真缺失备注写出两侧计数证据(如"译文页2出现10次 < 英文14次, 必须人工核对"), **不改档位**
- 判定取向(用户 2026-09-18 定调): 工具只做数字一致性对账、不辨语义; CO₂ 下标在译文被整词化(Koldioxidekvivalent) 同样升高风险转人工; 上/下标特征只能用于备注归因, 不得当作降档依据

### 回归 (2026-09-18, 以 v1.6 提交版 `2632024` 为 HEAD 对照, 逐语言四档 + 逐检查点向量对拍)
- F0295 红字+锚定 17 语言: 8004/29/96/4 **零扰动**(与 v1.6 基线一致)
- A0404 红字+锚定 24 语言: 181/0/6/10 **零扰动**
- J0105 高亮 184检查点×11 文件: 1901/0/119/4 **零扰动**
- J0105 红字+锚定 10 语言: 1227/11/17/1 → 1215/11/19/5, 12 条明细变化全部为"去掉假一致":
  6 条型号碎片转聚合灰名单(De/Es/Fr)、4 条 CO₂ 下标升高风险(Sv; 该件 EN 有 9 个≤6pt 下标 2, 除 Sv(5) 外其余 9 语言均为 9)、2 条 Tr 配对歧义降中风险
- 备注增强影响面: 仅 5 条真缺失获得计数证据(F0295 Es P7-24 + J0105 Sv 四条), 档位与计数与增强前 0 差异
- 对拍脚本自检: 今日 HEAD vs 9/15 `_回归_计数对账` 0 差异(口径复现正确); vs 9/14 `_校对结果_v5` 抓出 3212 条差异(脚本有效)

## v1.6 高亮模式 HTML 工作台 (2026-09-15)

- 高亮模式补齐与红字模式同等交付: 问题项双侧截图(EN 指示稿高亮区黄框 / XX 命中键字符级
  精确黄框, 键跨 span 退整行)、每问题一页的双语定位 PDF、数字校对报告.html(默认高风险
  视图/筛选按钮/点击定位)、Excel 含汇总/明细/高亮清单三 sheet
- 高亮清单.csv 被 Excel 占用时降级为提示(清单主载体在 xlsx sheet), 不阻塞报告
- v1.5 zip 为不完整版本(缺 HTML), 已删除, 以 v1.6 为准
- 验证: J0105 184检查点×11文件 HTML 全链路生成; F0295 红字回归 8004/29/96/4 零扰动

## v1.5 高亮模式字符级精确提取 (2026-09-15)
- 提取从"span 覆盖率≥50%"改为**字符级**: 长句中间只高亮一个词(如 'under 40 °C' 的 40,
  '1-2. SPECIFICATIONS' 的 1-2.)此前整 span 覆盖率仅 3~17% 必漏; J0105 检查点 92→184
- 词边界补全: 客户手绘高亮只涂型号中段('2G33VG')时向两侧扩展到完整词('MXZ-2G33VG')
- 行重建改为"先按 y 分行、行内按 x 排序": 下标(CO₂)yc 与正文差零点几 pt, 按 (yc,x0)
  全局排序会漂到行尾; 行内 x 排序天然归位
- code 键去尾标点变体(CN750. vs CN750)/逗号枚举拆分; 含可译小写词(3-core)归 phrase 按数字匹配
- 数字边界断言允许句尾点号(675.)但拒绝小数延续(0.85)
- 枚举型号跨行断词兜底('MXZ-' / '2HB50VF)'); 上下标/去空格检索变体
- 验证: J0105 基线一致175/184(残留9条中风险串位类), 注入三类全中; F0295 红字回归零扰动

## v1.4 高亮模式 (2026-09-15)

### 新增: 校对对象 = 指示稿红框内青色高亮内容(数字/型号/参数/序号), 未高亮不校
- 输入简化: 客户指示稿(红框+高亮) + 多国语文件夹, 不需英文红字稿/不需译文红字
- 提取纪律: 只信青色绘图小矩形; 面积>页面5%的"整页级注释"(编辑器全选残留)强制过滤;
  首跑输出「高亮清单.csv」人工核对提取范围后才可信结果
- 译文定位: 行级文本重建(型号被拆 span/与数字粘连时字面搜索必漏) +
  归一化检索(NFKC/连字符族/欧式逗号变体) + 页级y偏移与页码偏移双校准 +
  同页strict→loose→邻页 四级扫描消歧
- 类型策略: num/symstr/phrase 按数字token同行全含; code 子串+枚举拆分; ord 行带±40;
  toc(目录点线页码/脚注枚举) 全册值存在即一致(位置随重排不稳)
- 风险映射与红字模式四档对齐: 行带内=一致, 跨页/远距=中风险串位, 全册无=高风险真缺失
- 入口: CLI --mode highlight; GUI 顶部"校对模式"下拉; 输出 _高亮校对结果/(xlsx+清单csv)
- 验证(J0105, 92检查点×11文件): 基线一致88/92(残留4条为个位数字/局部页移, 中风险人工);
  合成注入3类全中(数字改0,8→0,9 / 型号改V6 / 整行删除); F0295 红字模式回归零扰动

### 已知边界(如实)
- 同串多处出现且仅一处错印: 会被其他正确出现"救回"为一致(型号类已按最近行带消歧, 残余风险低)
- 单数字值(0/7)天然歧义高, 行带外命中会进中风险供人工扫一眼
- 客户高亮画法(颜色/粒度)跨项目可能不同: 首跑核对清单为强制流程

## v1.3 高风险召回强化版 (2026-09-14~15)

### 核心变化
- **高风险重定义为「值级不可对账必报」**: 图内红字仅在值同页可对账时才降中风险, 值不存在也判高风险(捣回 El/Ro 图形 350 真缺失)
- **目录点线行对称降档**: 仅以英文侧判定目录页, 命中行标「目录条目(不校对)」退出计数; 不删项/不动权重(区别于旧提取层正则过滤会造假真缺失)
- **匈牙利分配修复**: 矩阵占位/上限统一为 REJECT_COST(旧 COST_BIG 占位会扭曲全局最优); 同值对 dx 权重 0.3 救回图形重排误报(De 30→2)
- **提取层**: 数字边界插空格(防 390|330 误粘成 390330); 脚注剔除加紧邻条件 gap≤3pt(修复 El/Ro 图形 350 被同行星号词误杀)
- **粘连注释标号**: 分支B 允许从译文多出项收养 + 第零遍已配对粘连前缀判一致(带同页冗余守卫), Fr/Pl/Hr 102 保持一致
- **报告交互**: HTML 默认只看高风险视图(可切换); 结论改「必办N·复核M·抽查K·框外差异P」; 框外值差异留痕计数(仅 token 数相等且逐位不等)
- **截图定位**: 译文多出项新增英文侧同位置参考截图 + EN 定位 PDF 加页(不再错跳译文PDF); 期望位置/同位置参考截图叠黄底红框(PIL, 与复核PDF同语义, 异常降级无框)

### 已验证 (F0295 锚定 17 语言)
- 植入错误框内 3/3 全中(Nl 980→985 / Es 50→55 / Pl 0.25→0,28), 框外 5/6 留痕
- 高风险最终 4 条 = 框内真差异 3 + Es 15 真缺失(原数据真问题); 中/低均值级可对账, 跳过不丢真错
- A0404(24 语言)与旧版逐语言一致, 零副作用; 匈牙利对拍 300 组通过

### 新增依赖
- Pillow(PIL): 仅用于截图叠黄底红框; spec hiddenimports 已加, excludes 已移除 PIL; 无 PIL 时自动降级为无框截图

## v1.2 四档风险版 (2026-09-01)

### 变更
- 状态体系改为四档风险分级(替代 不一致/需复核/大位移确认):
  - **一致**(绿): 匹配且数值等价, 无风险
  - **低风险**(蓝): 大位移确认(值相同但位置大幅移动, 插值+唯一候选确认), 抽查项
  - **中风险**(黄): 无法确认对应(串位/聚合/图内/跨页位移), 建议人工核对
  - **高风险**(红): 确认对应但数值不同(真差异), 或值在对面完全不存在(真缺失/多余), 必须处理
- HTML 筛选按钮按风险颜色显示(红/黄/蓝), 顶部统计四档卡片
- Excel 汇总/矩阵/说明同步四档
- 编号列对齐增加特征验证(防 0.25 被当编号 2 误配)
- 真缺失/真多余保留高风险(强信号), 同页有未配对候选则降中风险(串位)

### 已验证
- F0295 锚定 17 语言: 高风险 10(真实错误4+串位待人工过滤), 中风险 104, 低风险 38, 一致 10133
- 框内真实改错 4 个(980→985 / 50→55 / 0.25→0,28 / 15缺失) 全部捕获

## v1.0 全量扫描版功能(可完整复用)

### 输入
- 英文指示稿(全标红版) + 多国语文件(全部 DTP 标红,红色字体 span)

### 核心流程
1. 提取: 红色 span -> 注脚编号剔除 -> 行聚类(垂直同列/重叠拆分) -> 点号逗号聚合 -> 检查点
2. 匹配: 锚点估计页偏移 -> 匈牙利全局最优分配 -> 二级插值匹配(大位移) -> 头部/尾部单侧确认
3. 比较: 数值归一化(欧式小数/千分位) -> 一致/不一致/格式差异(点逗号写法)/待人工
4. 聚合差异识别(方向一~七): 译文聚合/英文拆散 | 英文聚合/译文拆散 | 数字串级联连写 | 区域成组 | 拆分+位移 | 排版换位
5. 跨语言交叉验证: 同检查点 >=3 语言同类聚合 -> 自动判一致(排版差异)
6. 输出: Excel(汇总/明细/矩阵/说明) + HTML(查错工作台,截图内嵌,可点击) + 复核PDF(高亮批注) + PNG截图

### 已验证效果
- A0400: 24 语言, 47 检查点(注脚剔除后), 不一致 0, 全部真实缺失/聚合正确识别
- F0295: 17 语言, 605 检查点, 不一致 15(真差异候选), 其余为聚合/缺失/多出类, 均已分类可追溯

### 关键参数(mee_checker.py 顶部可调)
- RED_* : 标准红/浅橙红阈值
- LINE_TOL=3.0 行聚类容差 / GAP_TOL=15 聚合间隔 / JOIN_GAP=2.0
- PRUNE_DY/DX 剪枝 / REJECT_COST=70 / SP_WIN=95 二级匹配窗口
- SNAP_DPI=150 截图分辨率

## 使用方式(如需回退)
```bash
git checkout HEAD -- mee_checker.py   # 恢复 git 首提交(v1.0)
# 或
python mee_checker_v1.0_全量扫描版_20260831.py --base <英文指示稿.pdf> --dir <多国语文件夹>
```
